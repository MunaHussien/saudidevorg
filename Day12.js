// where can we fine the resualt of the console.log(new Date());

console.log(new Date()); // we need to open develper tool using F12 or inspect any element, then in the Console tab , we past the code then pess enter.
