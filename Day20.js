var addNumber = 1;
while (addNumber <= 10) {
  document.writeln(addNumber);
  addNumber++;
}

var SubtractNumber = 10;
while (SubtractNumber > 0) {
  document.writeln(SubtractNumber);
  SubtractNumber--;
}
